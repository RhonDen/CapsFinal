
- Replace `import axios from 'axios'` with `import api from '../../api.js'`
- Use `api.post('/api/admin/logout')` instead of `axios.post('/api/admin/logout')`

## 3. Fix Logout (Navbar.jsx)
- Replace `import axios from 'axios'` with `import api from '../api.js'`
- Use `api.post('/api/admin/logout')` instead of `axios.post('/api/admin/logout')`

✅ No data will be erased

